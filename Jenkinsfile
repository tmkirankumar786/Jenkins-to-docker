pipeline {
  agent any
  stages {
      stage ('GIT Checkout') {
         steps {
           echo 'GIT Checkout Successful'
         }
      }
      stage ('Build Docker Image') {
         steps {
        
         }
      }
  }
}
